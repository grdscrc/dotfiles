# /home/unix/.config/tmuxinator/tex.yml

name: tex
root: ~/dev/eidos-export

windows:
  - editor:
      layout: main-vertical
      panes:
        - vim
        - wdocker && npm run test:integration
  - gpt(js): sgpt --repl js
  # - logs: tail -f log/development.log
